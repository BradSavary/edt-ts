{
  "name": "edt-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test-ar": "npm run test-ar --workspace=packages/scheduler-core",
    "test-standard": "npm run test-standard --workspace=packages/scheduler-core",
    "api:dev": "npm run dev --workspace=packages/scheduler-api",
    "api:start": "npm run start --workspace=packages/scheduler-api",
    "client:dev": "npm run dev --workspace=packages/scheduler-client",
    "client:build": "npm run build --workspace=packages/scheduler-client",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.18.3",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "~5.8.3"
  }
}


/**
 * Constantes partagées par l'heuristique MCV (getSchedulingPriority)
 * de TaskUnit et TaskGroupUnit.
 */

/** Poids appliqué à la contribution des dépendants transitifs dans le score MCV. */
export const DEPENDENTS_WEIGHT = 1;

# Build de production (`next build` avec output: 'export').
# Le client est servi sous basePath /edtts et Apache proxifie /edtts/api/* vers scheduler-api.
# Sans cette valeur, le bundle exporté appellerait /api/* à la racine du domaine → 404 en prod.
NEXT_PUBLIC_API_BASE=/edtts

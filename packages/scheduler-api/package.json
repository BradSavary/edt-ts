{
  "name": "@edt-ts/scheduler-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "npx tsx --watch src/index.ts",
    "start": "npx tsx src/index.ts",
    "build": "npx esbuild src/jobs/scheduler.worker.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/scheduler.worker.cjs --external:node:* && npx esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/server.cjs --external:esbuild",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.18.3",
    "esbuild": "*",
    "typescript": "~5.8.3",
    "vitest": "^3.0.0"
  }
}

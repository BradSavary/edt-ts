# Archive — moteur maison `scheduler-core`

Les documents de ce dossier décrivent le moteur de planification maison
(`@edt-ts/scheduler-core`, backtracking CSP + MCV + branch-and-bound), retiré du projet au
profit du seul moteur CP-SAT (`scheduler-cpsat`, OR-Tools). Voir
`docs/PlanSuppressionMoteurCore.md` pour le contexte de la suppression.

Le code correspondant reste consultable sur la branche `archive/dual-engine`.
